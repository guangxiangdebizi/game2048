import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.example.apple2048',
  appName: 'Apple 2048',
  webDir: 'www',
  bundledWebRuntime: false,
}

export default config